# /etc/profile - system-wide startup file of bash(1).

export LANG=en_US.UTF8
export PATH="/usr/local/sbin:/usr/local/bin:/usr/sbin:/usr/bin:/sbin:/bin"

if [ -d /etc/profile.d ]; then
	for i in /etc/profile.d/*; do
		if [ -f $i ]; then
			. $i
		fi
	done
	unset i
fi

# goto home directory
cd
